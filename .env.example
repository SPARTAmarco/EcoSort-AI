# Copia questo file in `.env` e inserisci la tua chiave.
# Il file `.env` NON viene mai committato (vedi .gitignore).
#
# Chiave gratuita da Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=la_tua_chiave_qui
